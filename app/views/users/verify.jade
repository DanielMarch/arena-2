extends ../shared/template

block content
  form(method='post', action='/verify/' + u._id)
    input(type='password', name='password', autofocus=true, placeholder='Enter Password', required='required')
    button Set Password
